package managers;

public enum CommandType {
    EXIT,
    HELP,
    ADD,
    INFO,
    SHOW,
    UPDATE,
    REMOVE_FIRST,
    REMOVE_BY_ID,
    PRINT_UNIQUE_GOVERNOR,
    FILTER_CONTAINS_NAME,
    ADD_IF_MAX,
    REMOVE_LOWER,
    PRINT_FIELD_DESCENDING_CAPITAL,
    CLEAR,
    EXECUTE_SCRIPT

}
